# parameter_store.py
# - Stores programmable parameter values.
# - Validates against constraints from the PACEMAKER doc.
# - Should be designed so values can later be “sent” to the pacemaker (Deliverable 2).